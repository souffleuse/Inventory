services:
  inventory:
    build: .
    container_name: inventory
    hostname: inventory
    ports:
      - "8080:80"
    volumes:
      # Persist JSON data outside the container
      - ./data:/app/data
    environment:
      - PORT=80
    restart: unless-stopped
